# first git
***
깃허브 사용법을 연습합니다.
***
여기에 원하는 내용을 입력하세요.
***
소스에 대한 간단한 소개글을 작성합니다.

`function add(x, y){ return x + y }`

```python
number = input("정수입력 > ")
number = int(number)

if number > 0:
  print("양수입니다")

if number < 0:
  print("음수입니다")

if number == 0:
  print("0입니다")
```
